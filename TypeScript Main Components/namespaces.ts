// a namespace for my code
export namespace MyCode {
    // a class exported, so it can be used outside of this namespace
    export class MyClass {
        // a static method
        public static myMethod() {
            console.log("Hello static world!");
        }
    }
}
